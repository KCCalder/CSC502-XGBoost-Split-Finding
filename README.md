# CSC502-XGBoost-Split-Finding

The XGBoost Split Finding is implemented in two seperate python notebooks, so that they can be easily run either on a single machine or in parallel. The single-machine implementation is titled 'XGBoost Split Finding' and the Spark based implementation is titled 'BenchMarkingWPySpark'. Both implementations can be run directly with Google Colab.
